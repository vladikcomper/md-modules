name: ConvSym

on:
  push:
    branches: [ "master" ]
    paths: [ "utils/core/**", "utils/convsym/**" ]
  pull_request:
    branches: [ "master" ]
    paths: [ "utils/core/**", "utils/convsym/**" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cd utils/convsym && ./build.sh
    - name: Test
      run: cd utils/convsym && ./test.py
 